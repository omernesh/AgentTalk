---
phase: quick-9-hebrew-voices-integration
task: code-review-fixes
status: in_progress
last_updated: 2026-03-01T15:09:55.455Z
---

<current_state>
Quick-9 implementation is complete and committed. A second manual code review was run
(4 agents in parallel). All results are in. Fixes have NOT been applied yet — context
ran out immediately after reading translator.py and user_prompt_hook.py.

The 4 review reports are in the task notification history above. All 36 issues are
catalogued and ready to fix.
</current_state>

<completed_work>

## Quick-9 execution (done ✓)
- Task 1: Voice namespace (service.py, tray.py, voice.md) — committed f06a682
- Task 2: translator.py + /speak integration — committed 4577d07
- Task 3: user_prompt_hook.py (Claude Code + opencode) + setup.py — committed e590407
- STATE.md + SUMMARY committed — edc8b04
- First code review autofix (2 fixes) — committed c7b799b
- Cleanup commit — bea5b27

## Second code review — COMPLETE, reports received
All 4 agents finished. Issues catalogued below.
</completed_work>

<remaining_work>

## Fix all 36 issues from the 4 review agents

### CRITICAL — fix first:
1. **translator.py line 73** — Wrong model ID `claude-haiku-4-5-20251001` → `claude-haiku-4-5`
2. **translator.py lines 90-93** — Bare `except Exception: pass` in notify block → `logger.debug(..., exc_info=True)`
3. **service.py ConfigRequest.voice** — Missing `min_length=1` on voice field
4. **hooks/user_prompt_hook.py line 45** — Bare `except Exception: pass` → add `print(f"...", file=sys.stderr)`
5. **integrations/opencode/user_prompt_hook.py lines 43-45** — Two bare passes → stderr + split JSONDecodeError from generic

### HIGH:
6. **service.py line 598** — `is_hebrew(req.text)` → `is_hebrew(" ".join(sentences))` (post-preprocess)
7. **tray.py "Active:" lambda** — Add lightblue/hebrewpiper branch: `f'Active: {state["voice"]} (Hebrew)'`
8. **hooks/user_prompt_hook.py `_config_path()`** — Use `_config_dir()` from config_loader (cross-platform)
9. **setup.py line 194** — `json.dumps(..., ensure_ascii=False)`
10. **translator.py** — Catch `ImportError` separately before generic `except Exception`
11. **setup.py except cleanup** — Wrap `tmp_path.unlink()` in try/except to prevent masking original exception
12. **service.py** — Validate `translate_to_hebrew()` return length before using
13. **translator.py line 79** — Guard `response.content[0]` with `if not response.content: raise ValueError`
14. **translator.py lines 80-86** — Validate all items are str: `if not all(isinstance(i, str) for i in result)`
15. **tray.py `_set_hebrew_voice`** — Log warning on None onnx_dir instead of silent return
16. **service.py update_config** — `isinstance(voice, str) and voice.startswith("he_")` instead of `.get("voice","").startswith()`
17. **setup.py** — `COMMANDS_SRC_DIR.exists()` check + protect copy loop with try/except
18. **setup.py** — Atomic write for path files (use .tmp + .replace() pattern like settings.json)

### MEDIUM/DRY:
19. **service.py + tray.py** — Extract `_hebrew_voice_path(onnx_dir, filename) -> str` helper (DRY the path construction)
20. **tray.py Model submenu** — Add lightblue/hebrewpiper radio buttons
21. **translator.py** — `_TRANSLATION_MODEL = "claude-haiku-4-5"` constant instead of inline string
22. **translator.py `is_hebrew()`** — Single-pass counter (no intermediate list)
23. **hooks (both)** — Add sync comment: `# SYNC: keep in sync with integrations/opencode/user_prompt_hook.py`
24. **opencode/user_prompt_hook.py** — Remove redundant `urllib.error.URLError` catch (covered by `except Exception`)

### LOW/ANNOTATIONS:
25. **translator.py** — `icon: "pystray.Icon | None" = None` type annotation
26. **tray.py** — `HEBREW_VOICE_MAP: Final[dict[str, str]] = ...` + `from typing import Final`
27. **tray.py** — Update `build_tray_icon` docstring to list Hebrew state keys
28. **tray.py** — `_draw_waveform bar_color: tuple[int, int, int, int]`
29. **service.py** — `onnx_dir: str | None = STATE.get("lightblue_onnx_dir")` explicit annotation
30. **service.py** — `voice_path: str | None = STATE.get("lightblue_voice_path")` in list_hebrew_voices
31. **service.py** — Add `400` to update_config responses decorator
32. **service.py** — Generalize list_voices docstring (he_* not hardcoded)
33. **setup.py** — `Path | None` not quoted string in register_hooks signature
34. **voice.md** — Add `**If a Hebrew voice selected (he_*):**` section header
35. **voice.md** — Add hebrewpiper voice selection section

### SKIP (too invasive for quick task scope):
- Moving HEBREW_VOICE_MAP to config_loader.py (would break the established import pattern)
- CONFIG_KEYS tuple refactor in service.py (touches too many unrelated lines)
- TypedDict for STATE (affects tts_worker.py which must not be touched)
- Splitting `_voice_items()` into sub-generators (low value, low risk)
</remaining_work>

<decisions_made>

- Model ID confirmed: `claude-haiku-4-5` (not `claude-haiku-4-5-20251001` which doesn't exist)
- HEBREW_VOICE_MAP stays in tray.py (not moving to config_loader — too invasive for quick task)
- TypedDict for STATE is out of scope (touches tts_worker.py which must not change)
- user_prompt_hook.py (Claude Code) keeps local file read (not HTTP) but should use _config_dir() for cross-platform
- All fixes should be in a single commit `fix(quick-9): comprehensive code review fixes`
</decisions_made>

<blockers>

None — all review reports received, fix list is clear.
</blockers>

<context>
This is a code review fix pass on quick task 9 (Hebrew voices integration).
The implementation is correct and working. All 4 parallel review agents completed.
The fix list above is the complete consolidated list of everything to change.

Files to edit:
1. agenttalk/translator.py
2. agenttalk/service.py
3. agenttalk/tray.py
4. agenttalk/hooks/user_prompt_hook.py
5. integrations/opencode/user_prompt_hook.py
6. agenttalk/setup.py
7. agenttalk/commands/voice.md
</context>

<next_action>
Start fresh session → /gsd:resume-work → apply all fixes above to the 7 files →
commit as `fix(quick-9): comprehensive code review fixes` →
run one final clean review pass to confirm zero issues.

The fix list in <remaining_work> is complete and ordered by priority.
Start with CRITICAL fixes (items 1-5), then HIGH (6-18), then MEDIUM/LOW.
</next_action>
