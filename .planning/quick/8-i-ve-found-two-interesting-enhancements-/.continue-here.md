---
phase: quick-8-hebrew-tts
task: testing
status: in_progress
last_updated: 2026-03-01T12:54:47.348Z
---

<current_state>
Quick task 8 (Hebrew TTS integration) is fully implemented, reviewed, and committed.
The AgentTalk service IS running (localhost:5050 returns {"status":"ok"}).
Next step: set up PiperStream and/or Light-BlueTTS to do live synthesis tests.
User asked to test both engines but neither Docker (PiperStream) nor Light-BlueTTS deps are set up.
</current_state>

<completed_work>

## Quick Task 8 — full implementation done

- HebrewPiperEngine (agenttalk/hebrewpiper_engine.py) — REST client wrapping PiperStream /synthesize/audio
- LightBlueTTSEngine (agenttalk/lightblue_engine.py) — local ONNX inference via hebrew_inference_helper
- tts_worker.py — engine dispatch, voice/lang routing, STATE keys for Hebrew config
- service.py — ConfigRequest extended, GET /hebrew-voices endpoint, startup restore loop
- config_loader.py — save_config persists all 14 fields including 5 Hebrew keys
- docs/hebrew-tts-setup.md — full user setup guide
- README.md — Hebrew TTS section added with both engine options

## Code review cycles completed (2 full rounds):

Round 1 (GSD auto):  voice routing bug (kokoro voice sent to Hebrew engines), exception handling
Round 2 (manual):    19 fixes including wave.open() leak, float epsilon comparison, is_ready ordering,
                     silent exception pass→log, failure counter reset on muted/blank, PermissionError
                     at ERROR level, orphan .json.tmp cleanup, stale docstrings, import ordering, etc.

All commits on main branch. Latest: be75bff (README), 561cd30 (code review fixes).
</completed_work>

<remaining_work>

## Testing (what we were about to start)

### HebrewPiper (PiperStream Docker)
1. Check if Docker is installed: `docker --version`
2. Check if PiperStream repo is already cloned anywhere on machine
3. If not: `git clone https://github.com/maxmelichov/PiperStream` somewhere
4. Download onnx.zip models per PiperStream README
5. `docker compose up` in the PiperStream directory
6. Test: `curl -X POST localhost:5050/config -d '{"model":"hebrewpiper"}'`
7. Test synthesis: `curl -X POST localhost:5050/speak -d '{"text":"שלום עולם"}'`

### Light-BlueTTS (local)
1. Check if torch is installed: `python -c "import torch"`
2. Install if needed: `pip install onnxruntime phonikud phonikud-onnx soundfile torch torchaudio`
3. Clone repo: `git clone https://github.com/maxmelichov/Light-BlueTTS`
4. Download 9 ONNX model files per Light-BlueTTS README
5. Add repo to PYTHONPATH
6. Test: `curl -X POST localhost:5050/config -d '{"model":"lightblue","lightblue_onnx_dir":"...","lightblue_voice_path":"..."}'`
7. Test synthesis with Hebrew text

### Fallback (if neither env is ready)
- Can still test /config endpoint accepts Hebrew settings
- Can test /hebrew-voices returns correct structure
- Can test engine switching updates STATE correctly
- Can test that error messages are actionable when deps are missing
</remaining_work>

<decisions_made>

- quick-8: HebrewPiperEngine uses /synthesize/audio (complete WAV) not /synthesize/stream
- quick-8: LightBlueTTSEngine defers all heavy imports to __init__
- quick-8: /hebrew-voices scans lightblue_voice_path parent dir for *.json dynamically
- Code review: float epsilon comparison (abs > 1e-9) for speed change detection
- Code review: is_ready set AFTER start_tts_worker() to prevent /health returning 200 before worker starts
</decisions_made>

<blockers>

- Neither PiperStream Docker nor Light-BlueTTS deps are set up on the dev machine yet
- Context limit hit at ~89% — paused before starting setup work
</blockers>

<context>
AgentTalk service running on localhost:5050 (confirmed). The Hebrew TTS code is complete
and reviewed. Testing requires external setup — either Docker for PiperStream or ~2GB torch
install for Light-BlueTTS. Start with Docker/PiperStream as it's simpler (no Python deps).

Check Docker first: if available, clone PiperStream, follow their README to get models,
then `docker compose up` and test synthesis. The service will lazy-load HebrewPiperEngine
on the first /speak call after switching model to "hebrewpiper".
</context>

<next_action>
Start with: `docker --version` to confirm Docker is available.
Then check if PiperStream is already cloned: `find /d /c -name "PiperStream" -type d 2>/dev/null | head -5`
If Docker ready → clone PiperStream, get models, docker compose up, test Hebrew synthesis.
If Docker not available → install Light-BlueTTS deps and test that path instead.
Also valid: test endpoints without synthesis (config, hebrew-voices, error messages).
</next_action>
