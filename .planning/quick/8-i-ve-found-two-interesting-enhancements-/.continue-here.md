---
phase: quick-8-hebrew-tts
task: testing
status: in_progress
last_updated: 2026-03-01T13:18:14.345Z
---

<current_state>
Testing Hebrew TTS engines. Service was restarted with updated code (quick-8 changes).
/hebrew-voices and /config with Hebrew fields are now confirmed working.
PiperStream Docker image is CURRENTLY BUILDING in the background (bo12hacz4 task ID — but this won't persist across sessions; Docker build may have completed or failed).
Light-BlueTTS deps partially installed.
</current_state>

<completed_work>

## Endpoint tests (no external deps) — PASSED
- /health → {"status":"ok"} ✓
- /config → returns all 14 fields including 5 Hebrew keys ✓
- /hebrew-voices → {"hebrewpiper":["male","female"],"lightblue":[]} ✓
- Service restarted to pick up quick-8 code (was running old version)

## PiperStream setup
- Cloned to D:\AI\PiperStream\
- Models downloaded to D:\AI\PiperStream\onnx\ (415MB total):
  - model.config.json (8.1K) ✓
  - phonikud-1.0.onnx (294MB) ✓
  - piper_medium_female.onnx (61MB) ✓
  - piper_medium_male.onnx (61MB) ✓
- Docker build started: `docker compose up --build -d` (was still running at session end)

## Light-BlueTTS setup
- Cloned to D:\AI\Light-BlueTTS\
- Installed: onnxruntime ✓, soundfile ✓, torchaudio ✓, phonikud_onnx ✓
- phonikud package (0.1.0): BROKEN LAYOUT — installs to src/model/ not as phonikud module
  - `import phonikud` fails even though pip show phonikud reports it installed
  - phonikud_onnx (Phonikud class for diacritization) IS importable ✓
  - `from phonikud import phonemize` fails — but Light-BlueTTS has graceful fallback (sets phonemize=None)
  - Without phonemize: can only handle pre-vocalized Hebrew (text with nikud) not raw Hebrew
- LightBlue ONNX models NOT downloaded yet (need: backbone.onnx, backbone_keys.onnx, text_encoder.onnx, reference_encoder.onnx, vocoder.onnx, length_pred.onnx, length_pred_style.onnx, stats.npz, uncond.npz)
- phonikud-1.0.onnx for LightBlue NOT downloaded yet
</completed_work>

<remaining_work>

## PiperStream (HebrewPiper engine) — next priority
1. Check if Docker build completed: `docker ps | grep piper` or `docker compose -f /d/AI/PiperStream/docker-compose.yml ps`
2. If not done: `cd /d/AI/PiperStream && docker compose up --build -d`
3. Wait for healthcheck: `curl http://localhost:8000/health` (expect {"status":"healthy",...})
4. Test direct PiperStream synthesis:
   ```
   curl -X POST "http://localhost:8000/synthesize/audio" -H "Content-Type: application/json" -d '{"text":"שלום עולם","model":"male"}' --output /tmp/test_male.wav
   ```
5. Switch AgentTalk to hebrewpiper: `curl -X POST localhost:5050/config -H "Content-Type: application/json" -d '{"model":"hebrewpiper","hebrewpiper_host":"http://localhost:8000","hebrewpiper_voice":"female"}'`
6. Test via AgentTalk speak: `curl -X POST localhost:5050/speak -H "Content-Type: application/json" -d '{"text":"שלום עולם"}'`
7. Verify audio plays through speakers

## Light-BlueTTS (lightblue engine)
1. Fix phonikud import — options:
   a. Try: `pip install git+https://github.com/thewh1teagle/phonikud` (may have correct layout)
   b. Or accept phonikud=None fallback (works with pre-vocalized text)
2. Download phonikud-1.0.onnx for LightBlue:
   ```
   curl -L "https://huggingface.co/thewh1teagle/phonikud-onnx/resolve/main/phonikud-1.0.int8.onnx" -o /d/AI/Light-BlueTTS/phonikud-1.0.onnx
   ```
3. Download ONNX models to D:\AI\Light-BlueTTS\onnx_models\:
   ```
   BASE="https://huggingface.co/notmax123/LightBlue/resolve/main"
   mkdir -p /d/AI/Light-BlueTTS/onnx_models
   for f in backbone.onnx backbone_keys.onnx text_encoder.onnx reference_encoder.onnx vocoder.onnx length_pred.onnx length_pred_style.onnx stats.npz uncond.npz; do
     curl -L "$BASE/$f" -o /d/AI/Light-BlueTTS/onnx_models/$f &
   done
   wait
   ```
4. Set PYTHONPATH to include Light-BlueTTS:
   `export PYTHONPATH=/d/AI/Light-BlueTTS:$PYTHONPATH`
5. Configure AgentTalk:
   ```
   curl -X POST localhost:5050/config -H "Content-Type: application/json" \
     -d '{"model":"lightblue","lightblue_onnx_dir":"D:/AI/Light-BlueTTS/onnx_models","lightblue_phonikud_path":"D:/AI/Light-BlueTTS/phonikud-1.0.onnx","lightblue_voice_path":"D:/AI/Light-BlueTTS/voices/female1.json"}'
   ```
6. Test synthesis: `curl -X POST localhost:5050/speak -H "Content-Type: application/json" -d '{"text":"שלום עולם"}'`

## After both engines tested
- Switch back to English: `curl -X POST localhost:5050/config -d '{"model":"kokoro"}'`
- Confirm: `curl localhost:5050/config | python -m json.tool`
- Mark quick-8 testing complete in STATE.md
- Delete .continue-here.md
</remaining_work>

<decisions_made>

- PiperStream models downloaded individually (not as onnx.zip — zip URL returned "Entry not found")
- HuggingFace repo: notmax123/piper-medium-heb has individual files: model.config.json, phonikud-1.0.onnx, piper_medium_male.onnx, piper_medium_female.onnx
- phonikud 0.1.0 has broken package layout — installs to src/model/ rather than as top-level phonikud package
- Light-BlueTTS TTSConfig uses: onnx_dir, phonikud_path, speed, use_gpu (no separate config_path required for our use)
- LightBlue HuggingFace repo: notmax123/LightBlue has 10 files (backbone.onnx, backbone_keys.onnx, text_encoder.onnx, reference_encoder.onnx, vocoder.onnx, length_pred.onnx, length_pred_style.onnx, stats.npz, uncond.npz + README)
- phonikud-onnx for LightBlue: from thewh1teagle/phonikud-onnx repo
</decisions_made>

<blockers>

- phonikud `phonemize` function: package layout broken — `import phonikud` fails. Light-BlueTTS falls back gracefully (phonemize=None), meaning only pre-vocalized Hebrew text will work. Raw Hebrew text will raise RuntimeError. Fix: try installing from git or use phonikud_onnx directly.
- AgentTalk service running via pythonw.exe (no console) — can't see service logs. If it crashes, check with `tasklist | grep python`.
</blockers>

<context>
All setup work done from scratch this session:
1. Restarted AgentTalk service to pick up quick-8 code — confirmed working with Hebrew endpoints
2. Cloned PiperStream, downloaded 415MB of models, started Docker build
3. Cloned Light-BlueTTS, installed most Python deps
4. Context ran out mid-build — Docker build was still running

The Docker build installs build-essential + ffmpeg + 350 packages inside the container (big image, ~10-15 min build time). It was in the apt-get install phase when we paused.

AgentTalk PYTHONPATH needs to include Light-BlueTTS directory for `from hebrew_inference_helper import HebrewTTS` to work. The service is running as pythonw.exe — need to set PYTHONPATH before restarting it, or modify the service launch to include it. Check tts_worker.py to see if there's a PYTHONPATH setup mechanism.
</context>

<next_action>
Start with: `docker ps | grep piper` or `cd /d/AI/PiperStream && docker compose ps` — check if the PiperStream build completed and container is running. If yes, test curl http://localhost:8000/health. If not, re-run `docker compose up --build -d` and wait.
</next_action>
